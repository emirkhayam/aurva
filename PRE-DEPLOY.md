# AURVA — Чек-лист готовности к деплою

_Обновлено: 2026-06-16. Источник: blind-QA прогон (14/14 PASS) + party-mode ревью команды._

## ✅ Готово
- [x] **Контент перенесён с aurva.kg** — новости (43 статьи + галереи, 150 фото), партнёры (4), структура ассоциации (15 человек), участники (11). Все медиа скачаны в наш Supabase Storage (бакет `uploads`), не хотлинк.
- [x] **Тёмная тема** на всех страницах (тоггл, no-flash, localStorage).
- [x] **Галерея-слайдер** (Swiper) на странице новости.
- [x] **Blind-QA** — 14/14 PASS (см. `BUGS.md`, `SUMMARY.md`).
- [x] **Админка пересобрана** — `npm run build` в `admin-panel`, свежий `dist` → `backend/public/admin/`. DEV-кнопки «Быстрый вход» в бандле больше нет (бэкап старой: `backend/public/admin-backup-*`).
- [x] **UX-фиксы** — навбар выровнен на `lg` (нет тесноты на планшете 768–1024px) на courses/charter/privacy/news-detail; логотипы партнёров на белых плитках (читаемы в тёмной теме).

## ⛳ Перед/во время деплоя
- [ ] **Тестовые аккаунты**: убедиться, что `testuser@aurva.kg` и `admin@aurva.kg` **отсутствуют** в боевом Supabase (dev-косметика скрыта по hostname, но аккаунты могут существовать в БД).
- [ ] **Docker-деплой**: при сборке через `docker-compose.production.yml` пересобрать образ `admin-panel` (его `Dockerfile` уже делает production-build → DEV-кнопки нет). Backend отдаёт свою `public/admin` — она теперь актуальна.
- [ ] **Smoke-тест на реальном домене** после деплоя:
  - главная (контент, карусель участников, новости),
  - `/news` + детальная новость (галерея-слайдер),
  - `/about` (структура + партнёры, тёмная тема),
  - `/cabinet` → редирект на вход,
  - `/admin/login` — форма входа, **без** DEV-кнопки, дашборд не утекает.

## 🗂 Бэклог (не блокеры)
- [ ] **Staging-tenant** для мутационного QA (регистрация, формы, админ-CRUD). Сейчас БД только боевая — авторизованные сценарии не прогонялись во избежание мутаций в проде.
- [ ] **CSP / Google Tag Manager**: GTM (`G-2M6V79H761`) тянет `ad.doubleclick.net`, CSP это блокирует (шум в консоли, на UX не влияет). Решить: отключить ad-features в GTM **или** добавить домен в `connect-src` (`backend/src/app.ts`).
- [ ] История деплоя непростая — сверяться с `COOLIFY_*` / `DEPLOY_*` доками в корне.

## Артефакты QA
`journeys.json` (14 сценариев) · `BUGS.md` · `SUMMARY.md` · `journey-results/` (отчёты + архивы прошлых прогонов).
